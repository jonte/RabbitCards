<UserControl x:Class="Cards.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" 
             xmlns:il="clr-namespace:Microsoft.Expression.Interactivity.Layout;assembly=Microsoft.Expression.Interactions"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" MouseUp="UserControlMouseUp" MouseDown="UserControlMouseDown" MouseLeftButtonDown="UserControl_MouseLeftButtonDown" MouseLeftButtonUp="UserControl_MouseLeftButtonUp" MouseMove="UserControl_MouseMove">
    <!--:Interaction.Behaviors> 
        <il:MouseDragElementBehavior x:Name="dragBehavior" x:FieldModifier="public" ConstrainToParentBounds="true" />
        <il:FluidMoveBehavior></il:FluidMoveBehavior>
    </i:Interaction.Behaviors--> 
    <Grid>
        <Viewbox Stretch="Uniform">
            <Border>
                <Border.Effect>
                    <DropShadowEffect></DropShadowEffect>
                </Border.Effect>
                <Canvas Name="canvas" Height="312.81" Width="224.23">
                </Canvas>
            </Border>
        </Viewbox>
    </Grid>
</UserControl>
